[project]
name = "pied-piper"
version = "0.1.0"
description = "Pied Piper AI project"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ruff",
    "ty",
    "import-linter",
    "vulture",
    "bandit",
    "xenon",
    "hypothesis",
    "mutmut",
    "semgrep>=1.0.0",
    "pytest",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
